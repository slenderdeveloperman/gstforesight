name: GST Foresight - Daily Ingest

on:
  schedule:
    - cron: '0 3 * * *'
  workflow_dispatch:

jobs:
  ingest:
    runs-on: ubuntu-latest
    timeout-minutes: 45

    permissions:
      contents: write

    steps:
      - name: Checkout
        uses: actions/checkout@v4
        with:
          token: ${{ secrets.GITHUB_TOKEN }}

      - name: Set up Python 3.11
        uses: actions/setup-python@v5
        with:
          python-version: '3.11'

      - name: Cache pip
        uses: actions/cache@v4
        with:
          path: ~/.cache/pip
          key: pip-${{ runner.os }}-${{ hashFiles('requirements.txt') }}
          restore-keys: pip-${{ runner.os }}-

      - name: Cache HuggingFace models
        uses: actions/cache@v4
        with:
          path: ~/.cache/huggingface
          key: hf-gte-small-v1
          restore-keys: hf-gte-small-

      - name: Install dependencies
        run: python3 -m pip install -r requirements.txt

      - name: Run ingest pipeline
        env:
          SUPABASE_URL: ${{ secrets.SUPABASE_URL }}
          SUPABASE_SERVICE_KEY: ${{ secrets.SUPABASE_SERVICE_KEY }}
          SARVAM_API_KEY: ${{ secrets.SARVAM_API_KEY }}
        run: python3 -m gst_foresight ingest --all

      - name: Generate predictions
        run: python3 -m gst_foresight predict

      - name: Refresh news ticker
        run: python3 scripts/scrape_ticker.py

      - name: Commit updated predictions + ticker
        run: |
          git config user.name  "github-actions[bot]"
          git config user.email "github-actions[bot]@users.noreply.github.com"
          git add data/predictions/latest.json data/news/ticker.json
          if git diff --cached --quiet; then
            echo "No changes to commit"
          else
            git commit -m "chore: update predictions + ticker [skip ci]

Automated ingest run: $(date -u '+%Y-%m-%d %H:%M UTC')"
            git push
          fi

      - name: Show status
        run: python3 -m gst_foresight status
